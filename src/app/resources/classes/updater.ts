export default class Updater {
         autoUpdate: boolean;

         /**
          * Starts the updater
          * @param {boolean} auto
          */
         constructor(
                  auto: boolean
         ) {
                  this.autoUpdate = auto;
         }
}